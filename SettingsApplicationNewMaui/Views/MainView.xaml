<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SettingsApplicationNewMaui.Views.MainView"
             xmlns:resources="clr-namespace:SettingsApplicationNewMaui.Localization"
             xmlns:local="clr-namespace:SettingsApplicationNewMaui.ViewModels" 
             Title="{resources:Translate Key=SelectLanguage}">

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Picker Title="{resources:Translate Key=SelectLanguage}" Grid.Column="0"
    ItemsSource="{Binding AvailableLanguages}"
    SelectedItem="{Binding SelectedLanguage}">
        </Picker>

        <Label Text="{resources:Translate Key=Greeting}" Grid.Column="1"
   VerticalOptions="Start" 
   HorizontalOptions="Start" />
    </Grid>

</ContentPage>